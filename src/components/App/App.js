import c from './App.module.css'
import CinemaPage from "../CinemaPage/CinemaPage";

function App() {
    return (
        <main className={c.App}>
            <CinemaPage/>
        </main>
    );
}

export default App;
